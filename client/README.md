# Client placeholder

This folder is reserved for a future frontend application (React/Vue/Svelte).

Structure suggestion:
- `src/` - frontend source
- `public/` - static assets

Right now the app uses server-side rendered PHP views in `server/views/`.
